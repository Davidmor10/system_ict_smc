// The address on /privacy and /terms.
//
// One constant rather than a literal in two files: a contact address that is
// right on one legal page and stale on the other is worse than either, and
// these are the two pages a person reaches when they want something from you.
export const OWNER_CONTACT_EMAIL = 'davidmor030908@gmail.com';

/** The legal name the terms are made with. A trading name until a company
 *  exists; replace it with the registered entity before charging at scale. */
export const OWNER_LEGAL_NAME = 'ONYX';
